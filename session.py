# coding: utf-8
get_ipython().run_line_magic('pwd', '')
get_ipython().run_line_magic('ls', '')
import EdgeStrength
import EdgeStrength
import skimage.data as data
from EdgeStrength import show
show(EdgeStrength.EdgeStrengths((255-camera)>0.5,3))
show(EdgeStrength.EdgeStrengths((255-data.camera())>0.5,3))
import EdgeStrength
show(EdgeStrength.EdgeStrengths((255-data.camera())>0.5,3))
EdgeStrength.EdgeStrengths
show(EdgeStrength.EdgeStrengths((255-data.camera())>0.5,3))
import EdgeStrength
show(EdgeStrength.EdgeStrengths((255-data.camera())>0.5,3))
import importlib
importlib.reload(EdgeStrength)
show(EdgeStrength.EdgeStrengths((255-data.camera())>0.5,3))
importlib.reload(EdgeStrength)
show(EdgeStrength.EdgeStrengths((255-data.camera())>0.5,3))
hitmissed = (EdgeStrength.EdgeStrengths((255-data.camera())>0.5,3))
hitmissed
importlib.reload(EdgeStrength)
camera = data.camera()
camera = 255-camera
show(camera)
camera = camera > 255/4
show(camera)
camera = 255-data.camera()
show(camera)
camera = camera > 100
show(camera)
EdgeStrength.EdgeStrengths(camera,3)
se
se = np.asarray([[1,1,1],[1,1,1],[1,1,1]])
import numpy as np
se = np.asarray([[1,1,1],[1,1,1],[1,1,1]])
importlib.reload(EdgeStrength)
EdgeStrength.EdgeStrengths(camera,se,3)
importlib.reload(EdgeStrength)
EdgeStrength.EdgeStrengths(camera,se,3)
importlib.reload(EdgeStrength)
EdgeStrength.EdgeStrengths(camera,se,3)
show(camera)
from skimage.feature import match_template
show(match_template(camera,SE))
show(match_template(camera,se))
show(camera)
match_template(camera,se)
np.argmax(match_template(camera,se))
sum(sum(match_template(camera,se)))
se
camera
np.argmax(match_template(camera==1,se))
camera
np.argmax(match_template(camera==1,se==1))
match_template(camera==1,se==1)
se
se == 1
match_template(camera,se==1)
a=np.asarray([1,1,1],[2,2,2]])
a=np.asarray([[1,1,1],[2,2,2]])
a
a[0:1]
a[0:2,0:2]
importlib.reload(EdgeStrength)
EdgeStrength.cornerMatch(camera,se,0)
importlib.reload(EdgeStrength)
EdgeStrength.cornerMatch(camera,se,0)
EdgeStrength.cornerMatch(camera,se)
argmax(EdgeStrength.cornerMatch(camera,se))
np.argmax(EdgeStrength.cornerMatch(camera,se))
show(EdgeStrength.cornerMatch(camera,se))
from skimage import filters
show(filters.sobel(EdgeStrength(camera,se,3)))
show(filters.sobel(EdgeStrength.EdgeStrengths(camera,se,3)))
importlib.reload(EdgeStrength)
show(filters.sobel(EdgeStrength.EdgeStrengths(camera,se,3)))
camera
camer.size()
camera.size()
camera.size
len(camera)
len(EdgeStrength.cornerMatch(camera))
len(EdgeStrength.cornerMatch(camera,se))
importlib.reload(EdgeStrength)
importlib.reload(EdgeStrength)
show(filters.sobel(EdgeStrength.EdgeStrengths(camera,se,3)))
show(filters.sobel(EdgeStrength.EdgeStrengths(camera,se,3)))
get_ipython().run_line_magic('history', '')
grep
get_ipython().run_line_magic('save', '')
get_ipython().run_line_magic('save', 'edgestrengthTestSession.py')
get_ipython().run_line_magic('hist', '')
get_ipython().run_line_magic('save', '-h test')
get_ipython().run_line_magic('save', '')
help save
help %save
get_ipython().run_line_magic('help', '')
get_ipython().run_line_magic('save', '-h')
